# Hermes Agent CLI Config Template
# Auto-generated by hermes-toolbox

model:
  default: "deepseek-chat"
  provider: "auto"

# Providers that auto-detect from .env:
# deepseek  -> DEEPSEEK_API_KEY + DEEPSEEK_BASE_URL
# kimi      -> KIMI_API_KEY
# openrouter -> OPENROUTER_API_KEY

terminal:
  backend: local
  timeout: 60
